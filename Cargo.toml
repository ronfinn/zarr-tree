[package]
name = "zarr-tree"
version = "0.2.0"
edition = "2024"
rust-version = "1.85"
description = "A small CLI for exploring the structure and metadata of local Zarr stores."
license = "MIT"
repository = "https://github.com/ronfinn/zarr-tree"

[dependencies]
serde_json = "1.0.151"
